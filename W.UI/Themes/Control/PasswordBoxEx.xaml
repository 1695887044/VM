<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cvt="clr-namespace:W.UI.Core.Converters"
    xmlns:local="clr-namespace:W.UI.Controls">
    <Style TargetType="{x:Type local:PasswordBoxEx}">
        <Setter Property="Background" Value="#FDFDFD" />
        <Setter Property="BorderBrush" Value="#22000000" />
        <Setter Property="BorderThickness" Value="1,1,1,2" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PasswordBoxEx}">
                    <Border
                        x:Name="MainBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="4"
                        RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <TranslateTransform X="0" />
                        </Border.RenderTransform>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                x:Name="PlaceholderPart"
                                Margin="12,0"
                                VerticalAlignment="Center"
                                Foreground="#66000000"
                                IsHitTestVisible="False"
                                Text="{TemplateBinding Placeholder}"
                                Visibility="Collapsed" />

                            <PasswordBox
                                x:Name="InternalPasswordBox"
                                Padding="{TemplateBinding Padding}"
                                VerticalContentAlignment="Center"
                                Background="Transparent"
                                BorderThickness="0"
                                Visibility="{Binding IsPasswordVisible, RelativeSource={RelativeSource TemplatedParent}, Converter={cvt:BooleanToVisibilityConverter}, ConverterParameter=Inverse}" />

                            <TextBox
                                x:Name="VisibleTextBox"
                                Padding="{TemplateBinding Padding}"
                                VerticalContentAlignment="Center"
                                Background="Transparent"
                                BorderThickness="0"
                                Text="{Binding Password, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                Visibility="{Binding IsPasswordVisible, RelativeSource={RelativeSource TemplatedParent}, Converter={cvt:BooleanToVisibilityConverter}}" />

                            <StackPanel
                                Grid.Column="1"
                                Margin="0,0,5,0"
                                Orientation="Horizontal">
                                <TextBlock
                                    x:Name="ErrorIcon"
                                    Margin="5,0"
                                    VerticalAlignment="Center"
                                    FontFamily="Segoe Fluent Icons"
                                    Foreground="Red"
                                    Text=""
                                    ToolTip="格式不正确"
                                    Visibility="Collapsed" />
                                <ToggleButton
                                    Width="30"
                                    Height="30"
                                    Margin="0,0,5,0"
                                    Cursor="Hand"
                                    Focusable="False"
                                    IsChecked="{Binding IsPasswordVisible, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border
                                                x:Name="BtnBorder"
                                                Background="Transparent"
                                                CornerRadius="4">
                                                <Grid>
                                                    <Path
                                                        x:Name="EyeIcon"
                                                        Width="16"
                                                        Height="16"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Data="M12,4.5C7,4.5,2.73,7.61,1,12c1.73,4.39,6,7.5,11,7.5s9.27-3.11,11-7.5C21.27,7.61,17,4.5,12,4.5z M12,17c-2.76,0-5-2.24-5-5s2.24-5,5-5s5,2.24,5,5S14.76,17,12,17z M12,9c-1.66,0-3,1.34-3,3s1.34,3,3,3s3-1.34,3-3S13.66,9,12,9z"
                                                        Fill="#99000000"
                                                        Stretch="Uniform" />
                                                    <Path
                                                        x:Name="SlashLine"
                                                        Width="16"
                                                        Height="16"
                                                        Data="M2,2 L22,22"
                                                        Stretch="Uniform"
                                                        Stroke="#99000000"
                                                        StrokeEndLineCap="Round"
                                                        StrokeStartLineCap="Round"
                                                        StrokeThickness="2"
                                                        Visibility="Collapsed" />
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="BtnBorder" Property="Background" Value="#15000000" />
                                                    <Setter TargetName="EyeIcon" Property="Fill" Value="#E6000000" />
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="SlashLine" Property="Visibility" Value="Visible" />
                                                    <Setter TargetName="EyeIcon" Property="Fill" Value="#0067C0" />
                                                    <Setter TargetName="SlashLine" Property="Stroke" Value="#0067C0" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsValid" Value="False">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="Red" />
                            <Setter TargetName="ErrorIcon" Property="Visibility" Value="Visible" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="MainBorder" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
                                            <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-3" />
                                            <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="3" />
                                            <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="0" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Password" Value="" />
                                <Condition Property="IsFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PlaceholderPart" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>