<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cvts="clr-namespace:W.UI.Core.Converters"
    xmlns:local="clr-namespace:W.UI.Controls">

    <Geometry x:Key="M.G.Link">M10.59,13.41C11,13.8 11,14.44 10.59,14.83L8.28,17.14C7.11,18.31 5.21,18.31 4.04,17.14C2.87,15.97 2.87,14.07 4.04,12.9L5.34,11.6C5.53,11.4 5.85,11.4 6.05,11.6L7.46,13C7.66,13.2 7.66,13.5 7.46,13.7L6.87,14.31C6.48,14.7 6.48,15.34 6.87,15.73C7.26,16.12 7.9,16.12 8.28,15.73L10.59,13.41M19.96,4.04C18.79,2.87 16.89,2.87 15.72,4.04L13.41,6.35C13,6.74 13,7.38 13.41,7.77C13.8,8.16 14.44,8.16 14.83,7.77L17.14,5.46C17.53,5.07 18.17,5.07 18.55,5.46C18.94,5.85 18.94,6.48 18.55,6.87L17.26,8.17C17.06,8.37 17.06,8.68 17.26,8.88L18.67,10.29C18.87,10.49 19.18,10.49 19.38,10.29L20.69,8.98C21.86,7.81 21.86,5.91 19.96,4.04M15,9L14.83,9.17L13.41,10.59L9.17,14.83L9,15C8.61,15.39 8.61,16.03 9,16.42C9.39,16.81 10.03,16.81 10.42,16.42L11.84,15L16.08,10.76L17.5,9.34C17.89,8.95 17.89,8.31 17.5,7.92C17.11,7.53 16.47,7.53 16.08,7.92L15,9Z</Geometry>
    <Geometry x:Key="M.G.Close">M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z</Geometry>
    <Geometry x:Key="Icon_Open">M14,3V5H17.59L7.76,14.83L9.17,16.24L19,6.41V10H21V3M19,19H5V5H12V3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V12H19V19Z</Geometry>

    <Style x:Key="IconButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Width" Value="24" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        x:Name="bd"
                        Background="{TemplateBinding Background}"
                        CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#15000000" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#30000000" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type local:GenericPropertyEditor}">
        <Setter Property="Background" Value="#F5F5F7" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:GenericPropertyEditor}">
                    <Border
                        x:Name="MainBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="#E0E0E0"
                        BorderThickness="0,0,0,1">
                        <Grid Margin="2,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="28" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Rectangle
                                x:Name="StatusBar"
                                Width="3"
                                HorizontalAlignment="Left"
                                Fill="Transparent" />

                            <Path
                                x:Name="StatusIcon"
                                Grid.Column="0"
                                Width="12"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{StaticResource M.G.Link}"
                                Fill="#CCCCCC"
                                Stretch="Uniform" />

                            <TextBlock
                                Grid.Column="1"
                                Margin="0,0,12,0"
                                VerticalAlignment="Center"
                                FontSize="12"
                                Foreground="#666666"
                                Text="{TemplateBinding Title}" />
                            <Grid Grid.Column="2">
                                <TextBlock
                                    x:Name="PART_Placeholder"
                                    VerticalAlignment="Center"
                                    Foreground="#BBBBBB"
                                    IsHitTestVisible="False"
                                    Text="{TemplateBinding Placeholder}"
                                    Visibility="Collapsed" />

                                <TextBox
                                    x:Name="PART_Input"
                                    VerticalContentAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    CaretBrush="#007ACC"
                                    FontSize="13"
                                    FontWeight="Medium"
                                    Foreground="#222222"
                                    Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}" />

                                <TextBlock
                                    x:Name="ComplexText"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    FontStyle="Italic"
                                    Foreground="#005A9E"
                                    Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}"
                                    Visibility="Collapsed" />
                            </Grid>

                            <StackPanel
                                Grid.Column="3"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <Button
                                    x:Name="PART_ClearBtn"
                                    Style="{StaticResource IconButton}"
                                    Visibility="{Binding CanClear, RelativeSource={RelativeSource TemplatedParent}, Converter={cvts:BooleanToVisibilityConverter}}">
                                    <Path
                                        Width="9"
                                        Data="{StaticResource M.G.Close}"
                                        Fill="#999999"
                                        Stretch="Uniform" />
                                </Button>
                                <Button
                                    x:Name="PART_LinkBtn"
                                    Command="{TemplateBinding LinkCommand}"
                                    Style="{StaticResource IconButton}"
                                    Visibility="{Binding IsLinkEnabled, RelativeSource={RelativeSource TemplatedParent}, Converter={cvts:BooleanToVisibilityConverter}}">
                                    <Path
                                        Width="11"
                                        Data="{StaticResource M.G.Link}"
                                        Fill="#007ACC"
                                        Stretch="Uniform" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsValid" Value="False">
                            <Setter TargetName="MainBorder" Property="Background" Value="#FFF0F0" />
                            <Setter TargetName="StatusBar" Property="Fill" Value="#D83B01" />
                            <Setter TargetName="StatusIcon" Property="Fill" Value="#D83B01" />
                            <Setter TargetName="PART_Input" Property="Foreground" Value="#D83B01" />
                        </Trigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsValid, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding Value, RelativeSource={RelativeSource Self}, Converter={cvts:StringNotNullToBoolConverter}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="StatusIcon" Property="Fill" Value="#007ACC" />
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding Text, ElementName=PART_Input}" Value="">
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <Trigger Property="IsEditable" Value="False">
                            <Setter TargetName="PART_Input" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ComplexText" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="#EDEDED" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>