<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="ModernTextBlockBase" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI, Arial" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="#334155" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style
        x:Key="M.S.Text.Label"
        BasedOn="{StaticResource ModernTextBlockBase}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#64748B" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Medium" />
    </Style>

    <Style
        x:Key="M.S.Text.Header"
        BasedOn="{StaticResource ModernTextBlockBase}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="#1E293B" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="2"
                    Direction="270"
                    Opacity="0.1"
                    ShadowDepth="1" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="M.S.Text.Value"
        BasedOn="{StaticResource ModernTextBlockBase}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="Consolas, Cascadia Code, Courier New" />
        <Setter Property="Foreground" Value="#007ACC" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style
        x:Key="M.S.Text.Description"
        BasedOn="{StaticResource ModernTextBlockBase}"
        TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="#94A3B8" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

</ResourceDictionary>