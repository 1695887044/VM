<UserControl x:Class="Plugin.DiplayData.Views.DisplayDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plugin.DiplayData.Views"
               xmlns:hc="https://handyorg.github.io/handycontrol"
            xmlns:prism="http://prismlibrary.com/"
             Height="400" Width="600"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml" />
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <hc:TabControl
            hc:IconElement.Height="16"
            hc:IconElement.Width="16"
            IsAnimationEnabled="True"
            IsTabFillEnabled="True">
            <hc:TabItem hc:IconElement.Geometry="{StaticResource PageModeGeometry}" Header="基本参数">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <GroupBox Header="图像索引">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock VerticalAlignment="Center" Text="索引:" />
                            <TextBox
                                Width="140"
                                Background="Transparent"
                                BorderThickness="0,0,0,2"
                                IsReadOnly="{Binding DelayTime.IsReadOnly}"
                                Text="{Binding DelayTime.Text}" />
                        </StackPanel>
                    </GroupBox>
                    <GroupBox Header="图像链接">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock VerticalAlignment="Center" Text="索引:" />
                            <TextBox
                                Width="140"
                                Background="Transparent"
                                BorderThickness="0,0,0,2"
                                IsReadOnly="{Binding DelayTime.IsReadOnly}"
                                Text="{Binding DelayTime.Text}" />
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </hc:TabItem>
            <hc:TabItem hc:IconElement.Geometry="{StaticResource TwoPageModeGeometry}" Header="显示设置">
                <GroupBox Header="图像参数" />
            </hc:TabItem>
        </hc:TabControl>
    </Grid>
</UserControl>
