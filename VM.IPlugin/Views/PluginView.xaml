<Window
    x:Class="VM.IPlugin.Views.PluginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:VM.IPlugin.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    ResizeMode="NoResize"
    AllowsTransparency="True"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#07C160" />
            <Setter Property="Width" Value="50" />
            <Setter Property="Height" Value="35" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Geometry x:Key="info">
            M512 224m-64 0a64 64 0 1 0 128 0 64 64 0 1 0-128 0Z M544 392h-64c-4.4 0-8 3.6-8 8v464c0 4.4 3.6 8 8 8h64c4.4 0 8-3.6 8-8V400c0-4.4-3.6-8-8-8z
        </Geometry>
    </Window.Resources>
    <Grid x:Name="PART_grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="auto"/>
            <RowDefinition Height="38" />
        </Grid.RowDefinitions>
        <StackPanel
            Grid.Row="0"
            Background="#6459DF"
            MouseLeftButtonDown="StackPanel_MouseLeftButtonDown"
            Orientation="Horizontal">
            <Path
                x:Name="IconCtl"
                Width="30"
                Height="30"
                Margin="5,4"
                Data="{Binding IconCtl}"
                Fill="Green"
                Stretch="Uniform" />
            <TextBlock
                x:Name="titleCtl"
                VerticalAlignment="Center"
                FontSize="26"
                Text="{Binding Title}" />
        </StackPanel>
        <ContentControl
            x:Name="PART_View"
            Grid.Row="1"
            VerticalContentAlignment="Stretch"
      VerticalAlignment="Stretch"
           />
        <DockPanel
            Grid.Row="2"
            Background="#6459DF"
            LastChildFill="True">
            <TextBlock
                x:Name="textBlock"
                Margin="10,2"
                VerticalAlignment="Center"
                DockPanel.Dock="Left">
                <Run Text="耗时:" />
                <Run x:Name="t1" />
                <Run Text="ms" />
                <LineBreak />
                <Run Text="状态:" />
                <Run x:Name="t2" />
            </TextBlock>
            <StackPanel
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                DockPanel.Dock="Right"
                Orientation="Horizontal">
                <Button Click="Button_Click" Content="执行" />
                <Button Click="Button_Click_1" Content="取消" />
                <Button Click="Button_Click_2" Content="确认" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
