<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ct="clr-namespace:VM.IPlugin.Controls">
    <Style TargetType="ct:EditValueBlock">
        <!--<Setter Property="Width" Value="240" />-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ct:EditValueBlock">
                    <Grid
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            Margin="2"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            FontSize="14"
                            FontWeight="Bold"
                            Text="{TemplateBinding Header}" />
                        <Grid Grid.Column="1">
                            <TextBox
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                Background="Transparent"
                                BorderBrush="White"
                                BorderThickness="0,0,0,1"
                                IsReadOnly="{TemplateBinding IsReadOnlay}"
                                Text="{Binding Path=CurrentValue, Mode=TwoWay, StringFormat='F2', UpdateSourceTrigger=LostFocus, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                TextWrapping="Wrap" />
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <Button
                                    Name="PART_IncreaseButton"
                                    MaxWidth="30"
                                    Margin="2"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Content="➕"
                                    FontWeight="Bold">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#234A64" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button
                                    Name="PART_DecreaseButton"
                                    MaxWidth="30"
                                    Margin="2"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Content="➖"
                                    FontWeight="Bold">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#234A64" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>