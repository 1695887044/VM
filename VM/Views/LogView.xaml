<UserControl
    x:Class="VM.Start.Views.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cvt="clr-namespace:VM.Start.Converts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:VM.Start.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="DarkGray"
    Opacity="0.6"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <hc:UniformSpacingPanel
            Margin="10,2,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            ChildWrapping="Wrap"
            Spacing="32">
            <hc:Badge
                Background="Green"
                BadgeMargin="0,-5,-10,0"
                Foreground="White"
                Text="{Binding service.LogSource[0].Count, Mode=OneTime}">
                <Button
                    hc:IconElement.Geometry="{StaticResource info}"
                    Command="{Binding OperatorCommmand}"
                    CommandParameter="info"
                    Content="信息"
                    Foreground="Green" />
            </hc:Badge>
            <hc:Badge
                Background="Orange"
                BadgeMargin="0,-5,-10,0"
                Foreground="White"
                Text="{Binding service.LogSource[1].Count}">
                <Button
                    hc:IconElement.Geometry="{StaticResource warn}"
                    Command="{Binding OperatorCommmand}"
                    CommandParameter="warn"
                    Content="警告"
                    Foreground="Orange" />
            </hc:Badge>
            <hc:Badge
                Background="OrangeRed"
                BadgeMargin="0,-5,-10,0"
                Foreground="White">
                <Button
                    hc:IconElement.Geometry="{StaticResource error}"
                    Command="{Binding OperatorCommmand}"
                    CommandParameter="Error"
                    Content="警告"
                    Foreground="OrangeRed" />
            </hc:Badge>
            <hc:Badge
                Background="Red"
                BadgeMargin="0,-5,-10,0"
                Foreground="White"
                Text="{Binding service.LogSource[3].Count}">
                <Button
                    hc:IconElement.Geometry="{StaticResource alarm}"
                    Command="{Binding OperatorCommmand}"
                    CommandParameter="Fatal"
                    Content="警告"
                    Foreground="Red" />
            </hc:Badge>
        </hc:UniformSpacingPanel>
        <DataGrid
            Grid.Row="1"
            Margin="1,0"
            Padding="1,0"
            AutoGenerateColumns="False"
            Background="#A8A8A8"
            BorderThickness="0"
            CanUserAddRows="False"
            CanUserResizeColumns="False"
            EnableColumnVirtualization="True"
            EnableRowVirtualization="True"
            FontSize="12"
            GridLinesVisibility="All"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding service.DisplayLogs}"
            RowHeaderWidth="20"
            SelectionMode="Single"
            SelectionUnit="FullRow"
            TextElement.Foreground="{Binding service.CrtType, Converter={cvt:LogToColorConvert}}"
            VirtualizingPanel.IsVirtualizing="True">
            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <Setter Property="BorderThickness" Value="1" />
                    <Setter Property="Padding" Value="5" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="20" />
                    <Setter Property="Padding" Value="5" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="#333333" />
                    <Setter Property="BorderBrush" Value="Black" />
                    <Setter Property="BorderThickness" Value="0.2" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#232323" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="Margin" Value="0,2,0,0" />
                    <Setter Property="Height" Value="20" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridColumnHeader">
                                <Border
                                    Padding="1"
                                    Background="#0078D7"
                                    BorderBrush="Black"
                                    BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="180" Header="时间">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding DateTime, StringFormat=\{0:yyyy/MM/dd HH:mm:ss fff\}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="80" Header="类型">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding logLevel}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="*" Header="内容">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock
                                Margin="5,0,0,0"
                                HorizontalAlignment="Left"
                                Text="{Binding Message}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
