<UserControl
    x:Class="VM.Start.Views.ProcessView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:VM.Start.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="Transparent"
    Foreground="White"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            x:Key="processListBox"
            BasedOn="{StaticResource ListBox.Small}"
            TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
            <Setter Property="ScrollViewer.PanningMode" Value="Both" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border
                            Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <ScrollViewer
                                Padding="{TemplateBinding Padding}"
                                CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}"
                                Focusable="False"
                                HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ScrollViewer>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsGrouping" Value="true" />
                                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                                </MultiTrigger.Conditions>
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Margin" Value="1" />
                        <Setter Property="Padding" Value="1" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border
                                        MinHeight="30"
                                        Padding="{TemplateBinding Padding}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        SnapsToDevicePixels="true">
                                        <ContentPresenter
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="ListBoxItem.IsSelected" Value="False" />
                                    <Condition Property="UIElement.IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="#574EB9" />
                                <Setter Property="Opacity" Value="0.5" />
                            </MultiTrigger>
                            <Trigger Property="ListBoxItem.IsSelected" Value="true">
                                <Setter Property="Background" Value="#574EB9" />
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>

                        </Style.Triggers>

                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.5*" />
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="3.5*" />
                                <ColumnDefinition Width="2*" />
                            </Grid.ColumnDefinitions>
                            <Path
                                Grid.Column="0"
                                Width="15"
                                Height="15"
                                Margin="5,1"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Stretch"
                                Data="{Binding IconText}"
                                Fill="White"
                                Stretch="Fill" />
                            <TextBlock Grid.Column="1">
                                <Run Text="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=(ItemsControl.AlternationIndex),Mode=OneWay}" />
                                <Run Text="." />
                                <Run Text="{Binding Name}" />
                            </TextBlock>
                            <TextBlock
                                Grid.Column="2"
                                Padding="5,0"
                                FontStyle="Italic"
                                Foreground="Green"
                                Text="{Binding Remark}" />
                            <TextBlock Grid.Column="3" >
                                <Run Text="{Binding CostTime}"/>
                                <Run Text="ms"/>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <ToolBar
            Grid.Row="0"
            Margin="2,10"
            Background="Transparent">
            <Button
                hc:IconElement.Geometry="{StaticResource start1}"
                Background="Transparent"
                BorderThickness="0"
                Foreground="Green"
                ToolTip="执行一次" />
            <Button
                hc:IconElement.Geometry="{StaticResource start2}"
                Background="Transparent"
                BorderThickness="0"
                Foreground="Green"
                ToolTip="连续执行" />
            <Button
                hc:IconElement.Geometry="{StaticResource stop1}"
                Background="Transparent"
                BorderThickness="0"
                Foreground="Red"
                ToolTip="终止流程" />
        </ToolBar>
        <ListBox
            Grid.Row="1"
            dd:DragDrop.DragHandler="{Binding}"
            dd:DragDrop.DropHandler="{Binding}"
            dd:DragDrop.IsDragSource="True"
            dd:DragDrop.IsDropTarget="True"
            dd:DragDrop.UseDefaultDragAdorner="True"
            dd:DragDrop.UseDefaultEffectDataTemplate="True"
            
            AlternationCount="10000"
            ItemsSource="{Binding ProcessDatas}"
            Style="{StaticResource processListBox}">
            <ListBox.ContextMenu>
                <ContextMenu FontFamily="{StaticResource iconfont}">
                    <MenuItem Header="重命名" Icon="&#xe6e3;" />
                    <MenuItem Header="编辑注释" Icon="&#xe61e;" />
                    <Separator />
                    <MenuItem Header="执行选中模块" Icon="&#xe67b;" />
                    <MenuItem Header="从此处执行" Icon="&#xe63d;" />
                    <MenuItem Header="显示所有" Icon="&#xe62b;" />
                    <MenuItem Header="启用超级工具" Icon="&#xe65d;" />
                    <Separator />
                    <MenuItem Header="设置断点" Icon="&#xe62c;" />
                    <MenuItem Header="模块参数" Icon="&#xe652;" />
                    <Separator />
                    <MenuItem Header="剪切" Icon="&#xe61b;" />
                    <MenuItem Header="复制" Icon="&#xe623;" />
                    <MenuItem Header="粘贴" Icon="&#xe624;" />
                    <MenuItem Header="禁用" Icon="&#xe627;" />
                    <MenuItem Header="删除模块" Icon="&#xe8b6;" />
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>
        <TextBlock Grid.Row="2" Margin="20,2">
            <Run Text="流程总耗时" />
            <Run Text="100" />
            <Run Text="ms" />
        </TextBlock>
    </Grid>
</UserControl>
