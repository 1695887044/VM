<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cs="clr-namespace:HalconDemo.ThemeCodes"
    xmlns:halcon="http://schemas.mvtec.com/halcondotnet">
    <Style TargetType="cs:ImageEdit">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="cs:ImageEdit">
                    <Grid Background="Black">
                        <!--<Grid.ContextMenu >
                            <ContextMenu IsEnabled="{Binding ContextEnable}" >
                                <MenuItem Header="ROI">
                                    <MenuItem x:Name="PART_Rectangle" Header="绘制矩形" />
                                    <MenuItem x:Name="PART_Ellipse" Header="绘制椭圆" />
                                    <MenuItem x:Name="PART_Circle" Header="绘制圆形" />
                                    <MenuItem x:Name="PART_Region" Header="绘制区域" />
                                    <MenuItem x:Name="PART_MASK" Header="创建屏蔽区" />
                                    <MenuItem x:Name="PART_Clear" Header="清空所有" />
                                </MenuItem>
                                <MenuItem Header="Display">
                                    <MenuItem x:Name="PART_DisplayInfo" Header="适应窗口" />
                                  
                                </MenuItem>
                            </ContextMenu>
                        </Grid.ContextMenu>-->
                        <TextBlock
                            x:Name="PART_TopText"
                            Margin="5"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Panel.ZIndex="1"
                            FontSize="18"
                            FontWeight="Bold"
                            Foreground="Red"
                            Text="{Binding TopText}" />
                        <halcon:HSmartWindowControlWPF x:Name="PART_Halcon" Panel.ZIndex="0" />
                        <TextBlock
                            x:Name="PART_BottomText"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Panel.ZIndex="1"
                            FontSize="18"
                            FontWeight="Bold"
                             Margin="5"
                            Foreground="Red"
                            Text="{Binding BottomText}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>